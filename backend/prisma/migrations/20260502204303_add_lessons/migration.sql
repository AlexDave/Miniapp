-- Superseded: `lessons` with module_id already created in 20260501235003_add_modules_lessons_reports.
-- This migration is a no-op to keep migration history linear; the old course_id + lesson_progress
-- design was not merged into the final schema.
SELECT 1;
