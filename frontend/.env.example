# API base URL (leave empty in dev — Vite proxy handles /api requests automatically)
# In production set this to your deployed backend URL, e.g.:
# VITE_API_URL=https://your-backend.example.com
VITE_API_URL=
